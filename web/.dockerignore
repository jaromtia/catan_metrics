# Rebuilt inside the image; never ship the host copies.
node_modules/
dist/
tsconfig.tsbuildinfo
tsconfig.node.tsbuildinfo
vite.config.js
vite.config.d.ts

# Container assets are added explicitly, not needed in the build context twice.
Dockerfile
.dockerignore
